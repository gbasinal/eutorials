import { ClickActiveDirective } from './click-active.directive';

describe('ClickActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickActiveDirective();
    expect(directive).toBeTruthy();
  });
});
